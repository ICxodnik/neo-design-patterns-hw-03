{
  "name": "factory-payments-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Payment system with multiple providers using Factory patterns",
  "author": "Nataliia Khodorova <icxodnik@gmail.com>",
  "main": "src/main.ts",
  "scripts": {
    "start": "npx ts-node src/main.ts",
    "build": "tsc",
    "build:start": "node dist/main.js",
    "format": "prettier --config .prettierrc.json 'src/**/*.ts' --write"
  },
  "dependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "prettier": "^3.6.2"
  }
}
